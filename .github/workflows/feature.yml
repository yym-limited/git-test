name: feature
on:
  pull_request:
    types: [opened, synchronize, edited]
    branches:
      - main
  workflow_run:
    workflows: ["main"]
    branches: [branch-*]
    types:
      - completed
jobs:
  build:
    # if: ${{ github.ref != 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'running'